<p id="notice"><%= notice %></p>

<h1>Bees</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>happy_bees_familyeee</th>
      <th>Picture</th>
      <th>Adress</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bees.each do |bee| %>
      <tr>
        <td><%= bee.name %></td>
        <td><%= bee.description %></td>
        <%= image_tag bee.picture_url, width: '100%' if bee.picture.present? %>
        <td><%= bee.adress %></td>
        <td><%= link_to 'Show', bee %></td>
        <td><%= link_to 'Edit', edit_bee_path(bee) %></td>
        <td><%= link_to 'Destroy', bee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bee', new_bee_path %>
